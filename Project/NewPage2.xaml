<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Project.VisualizzaSquadre"
             Title="Squadre Registrate"
             BackgroundColor="LightGreen">

    <ScrollView>
        <VerticalStackLayout x:Name="mainLayout" Padding="20" Spacing="20">
            <Label Text="Squadre e Giocatori" FontSize="24" HorizontalOptions="Center" TextColor="White" />
            <!-- Le squadre verranno aggiunte dinamicamente qui -->
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
